<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.oslab.mapper.UserMapper">
    <resultMap id="UserMap" type="com.example.oslab.entity.User">
        <id column="user_id" property="userId" />
        <result column="username" property="username" />
        <result column="password" property="password" />
        <result column="name" property="name" />
        <result column="create_time" property="createTime" />
    </resultMap>

    <select id="selectByUsername" resultMap="UserMap">
        select * from user where username = #{username}
    </select>

    <select id="selectById" resultMap="UserMap">
        select * from user where user_id = #{userId}
    </select>

    <insert id="insert" parameterType="com.example.oslab.entity.User" useGeneratedKeys="true" keyProperty="userId">
        insert into user (username, password, name, create_time)
        values (#{username}, #{password}, #{name}, now())
    </insert>
</mapper>
